---
version: alpha
name: "Profile Card UI Element.zip"
description: "Design system imported from a Claude Design handoff by Coast (D101), bundle v1."
colors:
  accent: "#0A84FF"
  avatar-gradient: "#0A84FF"
  avatar-gradient-end: "#5E5CE6"
  background: "#F2F2F7"
  button-text-on-accent: "#FFFFFF"
  card-surface: "#FFFFFF"
  neutral: "#E5E5EA"
  primary-text: "#1C1C1E"
  secondary-text: "#8E8E93"
typography:
  avatar-initials:
    fontSize: 24px
    fontWeight: 600
  button:
    fontSize: 15px
    fontWeight: 600
  name:
    fontSize: 18px
    fontWeight: 600
  role:
    fontSize: 14px
    fontWeight: 400
  stat-label:
    fontSize: 12px
    fontWeight: 500
  stat-value:
    fontSize: 17px
    fontWeight: 700
rounded:
  avatar: 30px
  button: 12px
  card: 20px
spacing:
  space14: 14px
  space18: 18px
  space2: 2px
  space24: 24px
  space3: 3px
  space4: 4px
---

# Profile Card UI Element.zip Design System

## Overview

Generated by Coast design intake (D101) from a Claude Design handoff on 2026-07-22T21:40:20.709Z, bundle v1. Design health: **excellent**. This file and design.html are generated mirrors of design/bundle.json — re-import regenerates all three together; do not hand-edit this file, change the source design and re-import instead.

## Colors

The palette carries 9 color, 6 typography, 6 spacing, and 3 radius token(s). Token names are the design's own. Value provenance is recorded per token in design/bundle.json (variable / captured / inferred).

## Typography

Each level below is authoritative — every fontFamily/fontSize/fontWeight in the built app must reference one of these levels, never an ad-hoc value.

## Layout

Spacing values form the layout scale. Every margin, padding, and gap snaps to a token — nothing one pixel off.

## Elevation & Depth

Elevation tokens are not extracted in this bundle version; follow the reference screenshots under design/screens/ for shadow and layering intent.

## Shapes

Corner radii come from the rounded scale above; apply them per component class consistently.

## Components

Component specs are not yet extracted as tokens. Per-screen structure lives in design/screens/<id>/context.json with a rendered reference alongside:

- **ProfileCard.dc** — reference: `design/screens/profile-card-dc/reference.png`, structure: `design/screens/profile-card-dc/context.json`

## Do's and Don'ts

- **Do** take every color, type, spacing, and radius value from these tokens (they seed the project's single theme file — D35).
- **Do** reuse an existing token when it fits; request a new theme-token plan item when none does.
- **Do** build each screen against its reference screenshot and structural context in design/screens/.
- **Don't** invent colors, type sizes, or spacing — if a needed value has no token, that is a plan item, not an inline literal.
- **Don't** edit anything under design/ — it is the founder's supplied design, written only by Coast at import (agents are refused by the wall).
- **Don't** treat this file as editable documentation; it regenerates from design/bundle.json on every import.
