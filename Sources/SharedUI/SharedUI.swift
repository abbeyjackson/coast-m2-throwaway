// Shared UI components live here (the uiLib path class, Sources/SharedUI/**).
// Every view in this target reads its formatting from the theme file seeded
// by the imported design — no view writes a colour, spacing or radius of its
// own.
import SwiftUI

/// Namespace marker for the shared UI component library.
public enum SharedUI {}
