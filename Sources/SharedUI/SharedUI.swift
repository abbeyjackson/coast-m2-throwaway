// Shared UI components live here (the uiLib path class, Sources/SharedUI/**).
// The engine builds views in this target against the theme seeded from the
// imported design. This scaffold just gives the target something to compile.
import SwiftUI

public enum SharedUI {}
