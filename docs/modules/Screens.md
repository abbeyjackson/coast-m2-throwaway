# Screens

Generated by Coast from the code (D110) — do not edit. Regenerated deterministically at every check; agents never write this file.

_Screens — the directory's home screen_

## What it does

The directory's home screen: the greeting, the section picker, and one card per member. One member's own screen: their card, and the note that opens it. The settings screen: which grouping home opens on, and signing out.

_Technical lens: 3 public types, 0 module-level public declarations — see the API reference._

## What it keeps

It keeps no data of its own.

_Technical lens: no data types conform to a coding/persistence protocol here_

## Who talks to it

Nothing else in the app uses this module yet. It builds on Directory, Greeter, and SharedUI.

_Technical lens: Screens → Directory, Screens → Greeter, Screens → SharedUI_

This module declares 3 public types and 0 module-level public declarations.

[API reference](../api/Screens-reference.md) · [class diagram](../diagrams/Screens-classes.md)

| Name | Kind | What it is |
|---|---|---|
| `HomeView` | struct | The directory's home screen: the greeting, the section picker, and one card per member. |
| `MemberProfileView` | struct | One member's own screen: their card, and the note that opens it. |
| `SettingsView` | struct | The settings screen: which grouping home opens on, and signing out. |
