# SharedUI

Generated by Coast from the code (D110) — do not edit. Regenerated deterministically at every check; agents never write this file.

_SharedUI — the one filled action button every screen uses_

## What it does

The one filled action button every screen uses. The profile card from the design: avatar, name and role, the stat row, and the follow button. Namespace marker for the shared UI component library.

_Technical lens: 5 public types, 0 module-level public declarations — see the API reference._

## What it keeps

It keeps no data of its own.

_Technical lens: no data types conform to a coding/persistence protocol here_

## Who talks to it

Used by Screens. It builds on Directory and Greeter.

_Technical lens: Screens → SharedUI, SharedUI → Directory, SharedUI → Greeter_

This module declares 5 public types and 0 module-level public declarations.

[API reference](../api/SharedUI-reference.md) · [class diagram](../diagrams/SharedUI-classes.md)

| Name | Kind | What it is |
|---|---|---|
| `PrimaryButton` | struct | The one filled action button every screen uses. |
| `ProfileCardView` | struct | The profile card from the design: avatar, name and role, the stat row, and the follow button. |
| `SharedUI` | enum | Namespace marker for the shared UI component library. |
| `StatBadgeView` | struct | One number and its label, as the profile card's stat row prints them. |
| `ThemeColor` | enum | Turns the theme's hex strings into SwiftUI colours, so every view reads its colours from the one theme file and never writes a literal. |
