# Directory

Generated by Coast from the code (D110) — do not edit. Regenerated deterministically at every check; agents never write this file.

_Directory — the three groupings the home screen offers_

## What it does

The three groupings the home screen offers. The store every directory screen reads from: the members, the section the reader is looking at, and the follow state they have changed. One person in the team directory.

_Technical lens: 3 public types, 0 module-level public declarations — see the API reference._

## What it keeps

It keeps no data of its own.

_Technical lens: no data types conform to a coding/persistence protocol here_

## Who talks to it

Used by Screens and SharedUI. It builds on Greeter.

_Technical lens: Screens → Directory, SharedUI → Directory, Directory → Greeter_

This module declares 3 public types and 0 module-level public declarations.

[API reference](../api/Directory-reference.md) · [class diagram](../diagrams/Directory-classes.md)

| Name | Kind | What it is |
|---|---|---|
| `DirectorySection` | enum | The three groupings the home screen offers. |
| `TeamDirectory` | class | The store every directory screen reads from: the members, the section the reader is looking at, and the follow state they have changed. |
| `TeamMember` | struct | One person in the team directory. |

## How it's wired inside

- `TeamDirectory` keeps `DirectorySection`
- `TeamDirectory` keeps `TeamMember`
