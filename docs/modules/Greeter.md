# Greeter

Generated by Coast from the code (D110) — do not edit. Regenerated deterministically at every check; agents never write this file.

_Greeter — a simple_

## What it does

A simple, stateless formatter that produces greeting strings. Namespace marker for the Greeter module (scaffold seed — the first feature's types arrive through the Coast pipeline). Theme tokens — seeded from the supplied designs (D101).

_Technical lens: 3 public types, 0 module-level public declarations — see the API reference._

## What it keeps

It keeps no data of its own.

_Technical lens: no data types conform to a coding/persistence protocol here_

## Who talks to it

Used by Directory, Screens, and SharedUI.

_Technical lens: Directory → Greeter, Screens → Greeter, SharedUI → Greeter_

This module declares 3 public types and 0 module-level public declarations.

[API reference](../api/Greeter-reference.md) · [class diagram](../diagrams/Greeter-classes.md)

| Name | Kind | What it is |
|---|---|---|
| `Greeter` | struct | A simple, stateless formatter that produces greeting strings. |
| `GreeterModule` | enum | Namespace marker for the Greeter module (scaffold seed — the first feature's types arrive through the Coast pipeline). |
| `Theme` | enum | Theme tokens — seeded from the supplied designs (D101). |
